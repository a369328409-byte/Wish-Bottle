# Wish Bottle

> a little jar for today's wishes

一个单文件的轻量级「攒钱 + 打卡 + 日记」小应用。完成任务赚取金币，攒够心愿金额就把它「拿下」，还可以每天写一篇随笔。

## 功能特性

### 数据看板
- **Saved**：当前已攒金额
- **Wishes**：心愿清单数量
- **Done**：累计完成任务数
- **Spent**：已用于实现心愿的金额

### 心愿清单
- 许愿：填写心愿名称、所需金额、备注（可选）
- 每个心愿显示攒钱进度条与百分比
- 攒够后可一键「🎉 拿下」：扣除存款、计入已花、移入已实现列表
- 已实现心愿支持「撤回」（退回金额）与折叠收纳
- 心愿可随时删除

### 日记本（按天）
- 日期导航：前后翻页查看任意一天，一键回到「今天」
- **今日任务**：
  - 点击「＋ 写个小任务」弹出小弹窗录入任务
  - 弹窗字段：任务名称、奖励金币（默认 10）、任务类型（分段选择）
  - 任务类型共五种：
    - **单次**：只在创建当天出现，完成后不再出现
    - **每日**：每天自动出现
    - **每周**：每周只在创建当天的星期几出现（如周一）
    - **每月**：每月只在创建当天的日期出现（如每月 1 号）
    - **自定义**：仅在设定的起止日期范围内出现，可填起始 / 截止日期
  - 同一循环任务在不同日期的完成状态相互独立，各天打卡互不影响
  - 完成任务获得奖励金额，取消完成自动扣回
- **今日随笔**：每天可保存一段文字

### 皮肤切换
- 六套配色皮肤：薰衣草紫（默认）、鎏金岁月、蜜桃轻粉、晴空浅蓝、薄荷生巧、星夜呢喃（深夜模式）
- 页面底部彩色圆点切换栏，圆点颜色对应各皮肤主色
- 皮肤偏好通过 localStorage 记忆，下次打开自动应用

### 响应式布局
- **移动端（<900px）**：单列纵向排列，与桌面端共用同一套卡片和弹窗样式
- **桌面端（≥900px）**：双列等高布局
  - 左列：数据看板 + 心愿清单；右列：日记本（跨两行）
  - 整体居中，两栏等宽，中间留 20px 间隔
  - 整体高度被视口限制（`calc(100vh - 40px)`），不随内容无限撑长
  - 任务列表和心愿清单固定高度（自适应视口），超出内容各自滚动
  - 左右栏底部严格对齐，窗口高矮自适应，最小可保证各显示 1-2 条

## 数据存储

所有数据保存在浏览器 `localStorage`，不依赖任何后端服务：

| 键名 | 内容 |
| --- | --- |
| `oc_goals` | 心愿清单 |
| `oc_achieved` | 已实现心愿 |
| `oc_cyclic` | 循环任务定义 |
| `oc_daily` | 按天的任务记录与随笔 |
| `oc_saved` | 已攒金额 |
| `oc_spent` | 已花金额 |
| `oc_theme` | 当前皮肤名称 |

> 注意：数据仅存在于当前浏览器中。清除浏览器数据、更换设备或浏览器后数据不会同步，请谨慎清理。

## 使用方式

无需构建、无需安装依赖，直接用浏览器打开 `index.html` 即可：

```bash
# 本地预览（可选）
python3 -m http.server 8000
# 然后访问 http://localhost:8000
```

也支持添加到手机主屏幕（已配置 apple-mobile-web-app 相关 meta），作为独立网页应用使用。

## 技术栈

- 纯原生 HTML / CSS / JavaScript，单文件实现
- 字体：Google Fonts（Cormorant Garamond + Noto Serif SC），数字优先使用本地宋体
- 图标：Font Awesome CDN
- 持久化：localStorage
- 布局：CSS Grid（桌面端双列）+ Flexbox（卡片内部自适应滚动）

## 📝 更新日志

### 2026-09-03
- ✨ 任务类型扩展为五种：单次 / 每日 / 每周 / 每月 / 自定义（弹窗内五段分段选择器）
- 🐛 修复「每周」任务每天都出现的问题：按创建当天的星期几锚定，每周仅该天出现
- 🐛 修复「每月」任务每天都出现的问题：按创建当天的日期锚定，每月仅该天出现
- 📅 「自定义」任务支持起止日期范围，仅在范围内出现（含首尾）
- 🎨 任务弹窗选择器改为统一分段控件样式，日期范围输入框美化（`[起始] 至 [截止]`）
- ✅ 同一循环任务在不同日期的完成状态相互独立，各天打卡与金币互不影响
- 🔧 旧数据兼容：无排期信息的老循环任务自动锚定到当前星期 / 今天

### 2026-09-01
- 🖥️ 新增桌面端双列响应式布局（≥900px）：左列数据看板 + 心愿清单，右列日记本
- 📐 整体高度自适应视口（`calc(100vh - 40px)`），两栏等高底部对齐
- 📜 心愿清单和任务列表改为固定高度 + 超出滚动，滚动条美化（5px 细条 + 主题色滑块）
- ⚡ 右栏新增任务高度自适应（flex:1 + min-height:0，消除 flexbox 默认 min-height 陷阱）
- 🎯 任务录入从 inline 输入框改为弹窗形式，与心愿弹窗风格统一
- ✏️ 任务弹窗字段：任务名称、奖励金币（默认 10）、任务类型（循环/单次 toggle）
- 🗑️ 删除旧的快捷输入 `+数字` 解析逻辑（弹窗有独立金币字段后不再需要）
- 💡 新增弹窗背景点击关闭（任务弹窗 + 心愿弹窗）

### 2026-08-29
- ✨ 新增六套皮肤切换功能（薰衣草紫、鎏金岁月、蜜桃轻粉、晴空浅蓝、薄荷生巧、星夜呢喃）
- 🌙 新增深夜模式「星夜呢喃」
- 🎨 页面底部新增皮肤切换选择器（彩色圆点）
- 💾 皮肤偏好支持 localStorage 记忆（`oc_theme`）
- 🎨 CSS 全量变量化，硬编码颜色统一改为 CSS 变量驱动
